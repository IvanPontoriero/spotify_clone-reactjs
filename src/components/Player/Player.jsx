import { Container } from './Player.elements'

const Player = ({ spotify }) => {

    return (
        <Container>
            <h1>Welcome to ur Spotify!</h1>
        </Container>
    )
}

export default Player;